name: magic-auth

services:
  api:
    build:
      context: .
      target: app
    command: npm run start:dev
    container_name: api
    ports:
      - '88:3000'
    restart: always
    volumes:
      - ./:/app
